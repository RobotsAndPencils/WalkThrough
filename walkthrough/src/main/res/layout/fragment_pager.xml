<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="context"
            type="android.content.Context" />

        <variable
            name="viewModel"
            type="com.robotsandpencils.walkthrough.presentation.main.paging.PagerViewModel" />

    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <android.support.v4.view.ViewPager
            android:id="@+id/view_pager"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <com.viewpagerindicator.CirclePageIndicator
            android:id="@+id/page_indicator"
            android:layout_width="match_parent"
            android:layout_height="@dimen/circle_indicator_height"
            android:layout_alignParentBottom="true"
            android:layout_gravity="bottom"
            android:layout_marginBottom="@dimen/circle_indicator_height"
            android:padding="@dimen/circle_indicator_gap"
            app:fillColor="@android:color/white"
            app:gapWidth="@dimen/circle_indicator_gap"
            app:radius="@dimen/circle_indicator_gap"
            app:strokeColor="@android:color/white" />

    </RelativeLayout>
</layout>

